import { InterviewViewer } from "@/components/interview-viewer";

export default function TypescriptDeepPage() {
  return (
    <InterviewViewer
      eyebrow="TypeScript Deep Dive"
      title="TypeScript Deep Interview Pack"
      description="Advanced TypeScript questions covering the type system, generics, inference, API design, runtime boundaries, and compiler-scale build strategy."
      sourcePath="/raw/typescript-deep"
    />
  );
}
